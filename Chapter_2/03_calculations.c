#include <stdio.h>

int main(){
    int a = 6 ;
    int b = 7 ;
    int c = a + b ;
    printf("the value of a %d,the value of b %d , and the sum is %d", a , b, c);
    // to understand modulus operator 
    printf("\n the value of a %d,the value of b %d , and the modulus is %d", a , b, a%b);

    return 0;
}