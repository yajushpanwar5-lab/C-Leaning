//  3.0 + 1 will be 
#include <stdio.h>

int main(){
    float x = 3.0 + 1 ;
    // result will be floating int because 3.0 is a floating point number and 1 is an integer so the result will be a floating point number

    printf ("the result with be %f", x);
    return 0;
}